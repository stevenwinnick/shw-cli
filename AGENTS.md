# shw-cli

## Testing Changes

After making changes, let the user know they can test them by running the install script. Provide the absolute path to `scripts/install.sh` within the repo root you're currently working in. For example:

```
<repo-root>/scripts/install.sh
```
